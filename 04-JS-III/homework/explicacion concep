//array: los array los podemos usar como cajas para guarda cualquier tipo de elemento adentro, cada elemento guardado tiene un orden numerico,
// el cual se puede usar para llamar a cada elemento, y usarlo como su fuera dicho elemento. 